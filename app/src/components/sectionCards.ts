import { createContext } from "react";

/** When true, each submission StepShell renders as a bordered SectionCard. Provided by the guided flow only. */
export const SectionCardsContext = createContext(false);
